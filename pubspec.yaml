name: like_minds_savings
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.3

  sign_up:
    path: ./packages/features/sign_up
  sign_in:
    path: ./packages/features/sign_in
  user_list:
    path: ./packages/features/user_list
  user_details:
    path: ./packages/features/user_details

  user_repository:
    path: ./packages/user_repository


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
